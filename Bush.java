import greenfoot.*;
public class Bush extends Actor
{
    public Bush()
    {
        GreenfootImage image = getImage();
        image.scale(image.getWidth() - 700, image.getHeight() - 500);
        setImage(image);
    }
    
    public void act() 
    {
        
    }    
}
